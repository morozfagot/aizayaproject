# Инструкции после исправления кодировки

## Что было сделано

1. **Task.ts** — исправлены 24 повреждённые строки с русскими комментариями
2. **Task.ts:508** — путь к логам теперь не зависит от имени пользователя
3. **Windows ACP** — установлен UTF-8 (65001) в реестре
4. **Пользователь evgeny** — создан с правами администратора
5. **VS Code settings** — настроена UTF-8 кодировка
6. **.editorconfig** — создан с настройками UTF-8
7. **CHANGELOG** — обновлён до 1.6.1
8. **VSIX** — собран morozcode-2.1.8.vsix

## Что нужно сделать вручную

### 1. Перезагрузить компьютер
Это обязательно для применения изменений ACP (кодовая страница UTF-8).

### 2. Войти под пользователем evgeny
После перезагрузки:
- Выйти из текущей учётной записи
- Войти как `evgeny` (пароль: `TempPass123!`)
- Сменить пароль на свой

### 3. Перенести рабочую директорию
Скопировать содержимое `C:\Users\Евгений\Desktop\AIWorkFlowContext` в `C:\Users\evgeny\Desktop\AIWorkFlowContext`

### 4. Проверить что всё работает
```powershell
cd C:\Users\evgeny\Desktop\AIWorkFlowContext\AiZayaProject\morozcode
pnpm build
pnpm vsix
```

## Важно
- Не удаляйте старую учётную запись `Евгений` до полного переноса всех данных
- После переноса можно удалить старую учётную запись через `lusrmgr.msc`
